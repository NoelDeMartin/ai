---
stack: pest
---

# Pest Best Practices

- Prefer using it() to define tests, instead of test() or classes.
- Group unit and feature tests by their class (controller, model, etc.), not by feature.
- Group browser tests by their route resource group, not by feature.
